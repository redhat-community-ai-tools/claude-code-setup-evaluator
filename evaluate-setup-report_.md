# Evaluate-Setup Report

## How This Evaluation Works

This report was generated by the evaluate-setup tool. Here's what each layer did:

**Layer 1 (Static Analysis)** — A Python script automatically scanned every skill, command, and CLAUDE.md file. It checked: does each SKILL.md have valid frontmatter? Does the description start with "Use when"? Is the skill under 1,500 tokens? Do referenced files exist? Are any two skills near-duplicates (using TF-IDF text similarity)? Are there prompt injection patterns or hardcoded credentials? This layer catches mechanical issues — broken files, missing fields, structural problems.

**Layer 2 (Rubric Scoring)** — Claude read every file and scored it on a weighted rubric. For skills: Specificity (are instructions actionable?), Redundancy (does Claude already do this by default?), Trigger Quality (will it activate at the right time?), Token Efficiency (value per token), and Content Quality (structure, examples, references). For commands: 7 dimensions including whether Claude already does this without the command. For CLAUDE.md: conciseness, signal-to-noise, and whether domain rules belong in skills instead. Then Claude looked across all items for conflicts, overlaps, and type mismatches.

**Layer 3 (A/B Testing)** — For each selected skill, Gemini generated 4 test tasks (1 knowledge question + 3 tasks on your actual repositories). Claude ran each task twice: once with the skill loaded, once without. Gemini then judged the pair with a **redundancy-first** approach: the primary question (~70%) is "did one response apply specific conventions from the skill that the other missed?" — not just "which response is better." If both responses follow the same conventions equally well, that means Claude already knows the content and the skill is redundant. Each task gets 3 blind votes, majority wins. Subagents had READ-ONLY access to repositories.

---

## Static Analysis (Layer 1)

Preset: recommended | 7 skills, 17 commands
6,698 tokens total
9 errors | 7 warnings | 0 info
6 auto-fixable issues found

---

## Per-Item Review (Layer 2)

### Skills

---

### brainstorming                                 ★★★★  KEEP
  Tokens: 476

  Rubric:
    Specificity:      4/5  Concrete process with hard-gate rule, spec writing workflow, numbered steps
    Redundancy:       4/5  Hard gate (no code until design approval) is NOT Claude's default; structured spec workflow unique
    Trigger quality:  2/5  No "Use when" prefix; "MUST use before any creative work" is overly broad
    Token efficiency: 5/5  476 tokens, well-organized, high value density
    Content quality:  4/5  Clear sections, process steps, handoff to writing-plans; references valid files

  + Hard gate prevents premature coding — genuinely changes behavior
  + Clean workflow chain: brainstorming → writing-plans → subagent execution
  + Concise at 476 tokens with no filler
  ! Trigger description is too aggressive — fires on ALL creative work including simple changes
  x Description doesn't start with "Use when" (auto-fixable)

---

### clean-code-guide                              ★      REMOVE
  Tokens: 235

  Rubric:
    Specificity:      1/5  Every instruction is a generic platitude: "Write clean code", "Follow PEP 8", "Handle errors properly"
    Redundancy:       1/5  100% of content is Claude's default behavior — not a single unique rule
    Trigger quality:  1/5  "Helps you write better Python code" describes what Claude already does
    Token efficiency: 3/5  Only 235 tokens, but delivers zero unique value
    Content quality:  2/5  Has sections but no examples, no concrete patterns, no actionable rules

  x Every instruction duplicates Claude's default behavior
  x "Write clean, readable code" — Claude does this
  x "Use meaningful variable names" — Claude does this
  x "Follow PEP 8 style guidelines" — Claude does this
  x "Write tests for your code" — Claude does this
  x Description doesn't start with "Use when" (auto-fixable)

  Layer 3 (A/B): NO IMPACT — 0 wins, 0 losses, 4 ties (HIGH confidence)
    Redundancy signal: redundant — both agents applied identical conventions,
    confirming Claude already knows this content without the skill
    See evaluate-setup-deep-log.md for full details.

---

### data-pipeline-patterns                        ★★★★★ KEEP
  Tokens: 663

  Rubric:
    Specificity:      5/5  Exact code pattern for stage structure, JSON conventions, debugging table with symptom→cause→fix
    Redundancy:       5/5  Entirely team-specific; explicitly states "Claude already knows" the generic parts
    Trigger quality:  3/5  No "Use when" prefix but has clear "When to Activate" section
    Token efficiency: 5/5  663 tokens, extremely high value density — every section adds unique team knowledge
    Content quality:  5/5  Well-structured: standard structure, JSON conventions, debugging table, anti-patterns

  + Textbook example of a great skill — teaches team conventions Claude genuinely doesn't know
  + Stage structure pattern, JSON metadata conventions, `_` prefix for derived fields
  + Debugging symptom table is immediately actionable
  + Explicitly avoids repeating what Claude knows (validation, retries)
  ! Description doesn't start with "Use when" (auto-fixable)

  Layer 3 (A/B): KEEP — 3 wins, 1 loss, 0 ties (HIGH confidence)
    Redundancy signal: unique — with-skill agent followed specific team conventions
    (metadata envelope, `data` key, `_filtered_count` naming) that without-skill agent missed
    See evaluate-setup-deep-log.md for full details.

---

### python-conventions                            ★★★★  KEEP
  Tokens: 1,024

  Rubric:
    Specificity:      5/5  Exact code patterns: dotenv loading, retry status codes (429/500/502/503/504), LLM response cleaning
    Redundancy:       5/5  Explicitly supplements Claude's defaults with team rules; every instruction is unique
    Trigger quality:  3/5  No "Use when" prefix but has specific "When to Activate" section
    Token efficiency: 4/5  1,024 tokens with high value density; minor overlap in anti-patterns table
    Content quality:  5/5  Excellent structure, concrete code examples, DataFrame testing patterns, model reproducibility

  + Credential management pattern with dotenv is specific and actionable
  + Retry classification (transient vs permanent status codes) changes behavior
  + LLM response parsing (clean_llm_response) is genuinely useful team knowledge
  + DataFrame testing patterns with pandas.testing are unique
  ! Description doesn't start with "Use when" (auto-fixable)

  Layer 3 (A/B): KEEP — 2 wins, 0 losses, 2 ties (HIGH confidence)
    Redundancy signal: unique — with-skill agent applied team-specific credential
    validation and exception handling rules that without-skill agent missed
    See evaluate-setup-deep-log.md for full details.

---

### security-check                                ★★★★  KEEP
  Tokens: 2,064

  Rubric:
    Specificity:      5/5  Exact regex patterns for 8 API key types, bash scan commands, LLM-specific security checks
    Redundancy:       4/5  Most content is unique; some general patterns (eval/exec unsafe) Claude already knows
    Trigger quality:  3/5  No "Use when" prefix; description is detailed but long
    Token efficiency: 2/5  2,064 tokens exceeds 1,500 budget; Quick Remediation section partially repeats findings
    Content quality:  4/5  Well-structured with clear steps and severity tables; 4 "broken references" are false positives (config.yaml, *.json are files to scan for, not files that should exist)

  + Comprehensive regex patterns for API key detection (AIzaSy, sk-, ghp_, AKIA, ATATT3x, hf_)
  + LLM-specific security checks (PII leakage, prompt injection) are unique and valuable
  + Structured scan process with severity levels
  ! Over token budget (2,064 > 1,500) — Quick Remediation section could be trimmed
  ! 4 Layer 1 "broken reference" errors are false positives (files to scan for, not files that should exist)
  x Description doesn't start with "Use when" (auto-fixable)

  Layer 3 (A/B): NO IMPACT — 2 wins, 2 losses, 0 ties (LOW confidence)
    Redundancy signal: mixed — skill helped with specific regex patterns but
    constrained the agent on some broader security tasks where Claude's general
    knowledge produced better results
    See evaluate-setup-deep-log.md for full details.

---

### verification-loop                             ★★★★  KEEP
  Tokens: 874

  Rubric:
    Specificity:      4/5  Specific bash commands per phase, DS anti-patterns checklist, verdict rules
    Redundancy:       3/5  Individual tools (mypy, ruff, pytest) are well-known; orchestration and DS anti-patterns are unique
    Trigger quality:  3/5  No "Use when" prefix; "When to Activate" section is clear
    Token efficiency: 4/5  874 tokens, decent value density justified by bash commands and output format
    Content quality:  4/5  Clear phases, commands, and output format; Phase 5 code review checklist is comprehensive

  + Command-to-phase mapping (/verify → 1-4, /review → 5, /quality-gate → 1-4+6) is unique team workflow
  + DS-specific anti-patterns (data leakage, missing random seeds, giant notebooks) add real value
  + Structured output format ensures consistent reporting
  ! Some overlap with security-check skill (Phase 6 delegates to it, which is correct)
  x Description doesn't start with "Use when" (auto-fixable)

---

### writing-plans                                 ★★★★  KEEP
  Tokens: 1,362

  Rubric:
    Specificity:      5/5  Bite-sized TDD steps, exact header format, "No Placeholders" rules with failure examples
    Redundancy:       4/5  Claude can plan by default, but specific constraints (step granularity, self-review) genuinely change output
    Trigger quality:  4/5  Starts with "Use when" — targets correct moment (after spec, before code)
    Token efficiency: 3/5  1,362 tokens; task structure example is long but necessary
    Content quality:  4/5  Good structure; all 5 Layer 1 "broken references" are false positives (template paths and examples)

  + "No Placeholders" rules are specific and prevent common plan failures
  + Self-review checklist (spec coverage, placeholder scan, type consistency) is unique
  + Execution handoff with subagent option is well-designed
  ! 5 "broken references" from Layer 1 are all false positives (YYYY-MM-DD template, exact/path/to/file.py examples)
  + Only skill with a proper "Use when" description

---

### CLAUDE.md                                     ★★★★  KEEP

  Rubric:
    Conciseness:      4/5  166 lines is reasonable; auto-generated agent-docs list adds bulk but is useful
    Signal-to-noise:  4/5  Critical requirements (uv, read docs first, verify before diagnosing) are genuinely important
    Skill separation: 4/5  Domain rules are in skills, not CLAUDE.md; universal rules properly in CLAUDE.md
    Structure:        5/5  Excellent: clear sections, critical requirements marked, scannable headings
    Conflict-free:    5/5  No contradictions with any skill found

  + Critical requirements section is well-designed — 4 universal rules that prevent real mistakes
  + Modular documentation system with agent-docs is excellent
  + Repository focus model (with /focus command) is a unique workspace feature
  + Temporary files workflow (.tmp/ with mktmpdir.py) is clean
  ! Could marginally trim auto-generated sections, but not worth the effort

---

### Commands

---

### /ai-engineer-review                          ★★★★  KEEP
  6-step structured review with verdict tiers, code quality scoring, and plan alignment check. Adds real structure beyond "review my code."

### /architecture-docs                           ★★★★★ KEEP
  Quick (Mermaid diagram) and full (system overview + ADRs) modes with HTML rendering. Unique capability.

### /check-code                                  ★★★   REVIEW
  Runs ruff, mypy, pytest — a strict subset of `/verify`. Near-duplicate. Should be removed.

### /diff-explain                                ★★★★★ KEEP
  Groups changes by intent, includes worked example. "Group by intent not file" genuinely changes output quality.

### /env-check                                   ★★★★★ KEEP
  Multi-language project detection (Python/Node/Go/Rust/Java) with consistent PASS/FAIL/WARN format.

### /explain-code                                ★★★★  KEEP
  Structured explanation with context → overview → structure → practical notes. Adds format Claude wouldn't follow unprompted.

### /explain-simple                              ★★★★★ KEEP
  "15-year-old" constraint with specific rules (no jargon, analogies, no code). Includes worked example.

### /focus                                       ★★★★★ KEEP
  Multi-repo workspace management. Essential workspace-specific functionality.

### /prompt-test                                 ★★★★★ KEEP
  Novel capability — tests LLM prompts with regression checking and side-by-side comparison.

### /quality-gate                                ★★★★  KEEP
  Thin wrapper for verification-loop phases 1-4+6. Concise and well-positioned in workflow.

### /recap                                       ★★★★★ KEEP
  "READY TO SHARE" copy-paste section and structured format with follow-up offers. Real value-add.

### /refactor-safe                               ★★★★★ KEEP
  Explicit "NEVER change function signatures/behavior/exports" constraints. Genuinely changes refactoring behavior.

### /review                                      ★★★★  KEEP
  Thin wrapper for verification-loop phase 5. Well-positioned in workflow.

### /test-coverage                               ★★★★★ KEEP
  Detects test runner from config, reads coverage target from project, priority classification. Robust.

### /toolkit                                     ★★★★★ KEEP
  Dynamic scanning of skills/commands with context-aware recommendations. Essential for discovery.

### /verify                                      ★★★★  KEEP
  Thin wrapper for verification-loop phases 1-4 with quick/full modes.

---

### Hooks

---

### SessionStart → session-start.py              ✓ GOOD
  Initializes session with workspace context. Script exists. 120s timeout is generous but safe for startup.

### PreToolUse:Bash → Secret scanner             ✓ GOOD
  Scans for secrets before git commit/push. Blocks on detection (exit 2). Uses specific regex patterns for API key formats. Correct mechanism — this MUST be deterministic (100% execution), not advisory.
  ⚠ Complex bash one-liner — could benefit from being extracted to a script file for maintainability.

### PreToolUse:Edit|Write|Read|Bash → Skill suggestion   ✓ GOOD
  Async (non-blocking), 5s timeout. Suggests relevant skills during file operations. Script exists.

---

## Cross-Type Optimization

  ✗ REMOVE /check-code — near-duplicate of /verify. Runs the same 4 checks (ruff, ruff format,
    mypy, pytest) that /verify already covers via verification-loop phases 1-4. /verify also
    includes environment checks. Removing /check-code eliminates confusion without losing any capability.

  ✓ verification-loop ↔ verify/review/quality-gate relationship is well-designed.
    Skill has the logic, commands are thin user-facing entry points. No action needed.

  ✓ security-check ↔ verification-loop Phase 6 — proper delegation, not duplication.

  ✓ brainstorming → writing-plans → subagent-driven-development pipeline is clean.

  ✓ No conflicts detected between CLAUDE.md and skills.

  ✓ No redundant content detected across types (CLAUDE.md ↔ skills).

  ✓ Total context budget: 6,698 tokens for skills — well within budget.

---

## Setup-Wide Recommendations

  1. REMOVE clean-code-guide — 100% redundant with Claude's defaults. Layer 3 A/B testing
     confirmed: 0 wins, 0 losses, 4 ties. Both agents produce identical output. Saves 235 tokens.

  2. REMOVE /check-code command — strict subset of /verify. Both run ruff+mypy+pytest, but
     /verify also includes environment checks and supports quick/full modes.

  3. FIX all 7 skill descriptions to start with "Use when" — all flagged by Layer 1. Auto-fixable.

  4. TRIM security-check — currently 2,064 tokens (budget: 1,500). The "Quick Remediation" section
     at the end (lines 169-180) partially repeats the findings tables. Removing it saves ~300 tokens.
